# Open Questions Tracker

## autopilot-impl-phase2 — 2026-06-05

- [ ] Pipeline backfill omission — Phase 2 PipelineSyncManager.backfill is a no-op (webhook-driven only). Acceptable for Phase 2 because pipeline state churns fast; promote to Phase 3 if drift observed. — Affects observability of CI status on bindings where the pod was offline during pipeline runs.
- [ ] Shared `'notes'` cursor across issue-notes and MR-notes — both paths share one cursor; worst case is one redundant fetch per backfill cycle. Split into `'issue_notes'` + `'mr_notes'` in Phase 3 only if performance metrics demand. — Affects backfill efficiency on note-heavy bindings.
- [ ] Mixin removal on type change — if a Huly user changes an MR-mirror Issue's type to "not an MR," do we strip the `gitlab-mr` mixin? Default: no; mixin persists; only GitLab-side delete removes the mirror. — Affects UX consistency when Huly users edit mirror Issues directly.
- [ ] `reviewers` mapping as comma-separated labels (`gitlab:reviewer:<username>`) — Phase 2 ships as labels; Phase 3 should introduce a typed reviewer field. — Affects fidelity of approver/reviewer modeling vs raw labels.
- [ ] `createMergeRequest` callable from tests but not production — `applyLocal` does not call it in Phase 2 (deferred to Phase 3 when intent capture lands). — Affects expectations for users who edit a Huly-only "MR-like" Issue and expect it to push.
- [ ] One-time webhook re-registration for existing Phase 1 bindings — documented as manual admin operation via `POST /api/v1/bindings/:id/re-register-webhook`. Autonomous batch migration deferred to Phase 3. — Affects operators with active Phase 1 deployments who must run the runbook to receive MR + pipeline events.
- [ ] Runtime mixin `gitlab-mr` viability without model registration (P2-R1 contingency) — if E2E mixin read fails against the real Huly transactor, fall back to JSON-string custom field on the Issue. Confirm during P2-T-07 / P2-T-11. — Affects whether the chosen data model survives contact with the real platform; contingency adds ~200 LOC to P2-T-07.

## autopilot-impl-phase3 — 2026-06-06

- [ ] Discussion-only events on Note Hook — GitLab Note Hook delivers discussion meta with note events; Phase 3 default assumes that's sufficient. If discussion-only state changes (resolved/unresolved without note body changes) don't fire Note Hook, fall back to polling via `listDiscussions` during backfill. — Affects whether resolve/unresolve actions made on GitLab without a comment edit propagate to Huly between backfill cycles.
- [ ] Approval action attribution fallback — per-user OAuth tokens are preferred for the `approveMR`/`unapproveMR` actor identity; Phase 3 ships with a stub `resolveActorToken` returning `undefined`, so all approvals fall back to the service-account credential with a warn log. Phase 4 should ship a Huly UI for per-user OAuth linkage. — Affects audit-log accuracy on GitLab: approvals appear as the bot user, not the real Huly user.
- [ ] `MR_MIXIN` field-count growth — now 13+ fields after Phase 3 additions (reviewers, approvedBy, approvalsRequired, approvalStatus, diffWebUrl, changedFiles). Default: keep as single mixin per "three similar lines is better than premature abstraction." If Phase 4 grows further, split into `gitlab-mr-core` + `gitlab-mr-review`. — Affects maintainability and the field-ownership invariant enforcement burden.
- [ ] Field-ownership invariant enforcement — Phase 3 carries forward Phase 2 critic C2 informally (JSDoc partition note + per-task tests). A compile-time partitioning helper (`MRMixinFields`) would prevent regressions where a future edit to `MergeRequestsSyncManager.applyRemote` accidentally touches `pipelineStatus` or `gitlab-review` fields. Deferred to Phase 4. — Affects long-term safety of the partitioned-mixin design under future contributors.

## autopilot-impl-phase4 — 2026-06-06

- [ ] TxSubscriber subscription path (A vs B vs C) — P4-T-01b investigates `@hcengineering/core` for `client.notify(handler)` (Path A), `client.addTxHandler` (Path B), or polling fallback (Path C). Default assumed: Path A. Escalate if probe finds significant vendor.d.ts widening required or Path C is the only option (adds a `tx_cursors` state collection and 5s polling cadence). — Affects whether Phase 4's Path B closure ships on schedule and how much engine surface area changes.
- [ ] Cookie `ServerSecret` rotation strategy — cookies HMAC'd with one secret stop verifying after rotation. Phase 4 default: maintain a `ServerSecretPrev` env var for grace-period dual-verification (current first; fall back to prev). NOT implemented in Phase 4 unless P4-T-14 reviewer requires it; track as post-Phase-4 maintenance. — Affects deployment rotation operations and user-OAuth UI availability during secret rotation windows.
- [ ] `mr.ts` mixin split execution (Phase 3 Q5 trigger reached) — with Phase 4 adding `approvalRules`, `iteration`, `parentEpicIid` (3 new fields), the `gitlab-mr` mixin reaches ~17 fields, surpassing the Phase 3 Q5 trigger (≥ 4 more = split). Phase 4 ADR (P4-T-21) defers the split as a post-Phase-4 maintenance ticket: `gitlab-mr-core` + `gitlab-mr-review` + `gitlab-mr-ee`. — Affects long-term maintainability and the JSDoc-only field-ownership invariant.
- [ ] EE compose stack for E2E — P4-T-20 EE tests auto-skip when `capabilities.edition === 'CE'` is detected. Default: maintainers run the EE test variant against a local EE compose periodically; CI runs CE-only. Track an EE CI runner as a post-Phase-4 nice-to-have. — Affects ongoing EE coverage confidence between manual EE runs.
- [ ] TxSubscriber polling fallback details (Path C only) — if P4-T-01b chooses Path C, Phase 4 adds a `tx_cursors` MongoDB collection with workspace-scoped rows and a default 5s poll cadence. Cadence and cursor format should be tuned against real workload metrics post-deploy. — Affects load on the Huly transactor under polling cadence and lag of Huly → GitLab applyLocal propagation.

## autopilot-impl-phase5 - 2026-06-06
- [ ] Service-account PersonId API surface (P5-T-01b) — Path A `findPersonBySocialKey('system:account')` vs Path B `getSystemAccount` vs Path C `findPersonByEmail` vs Path D no-API; gates P5-T-04. If Path D: spec §A is blocked.
- [ ] Legacy `mr-mixin.ts` deletion timing — Phase 5 retains for read-compat; deletion is a separate post-migration cleanup PR (not Phase 5 scope).
- [ ] `graphql-request` package version + transitive deps — verify present in package.json before P5-T-21 starts (per Phase 1 spec).
- [ ] Webhook secret rotation scope (P5-T-07) — does `webhook.ts` use pod-wide `ServerSecret` or per-binding tokens only? If per-binding only, P5-T-07 reduces to documentation.
- [ ] Huly platform mixin-removal API (P5-T-19 step 7) — verify `updateDoc({$unset: {[MR_MIXIN]: ''}})` actually removes the mixin tx-level. If unavailable: document post-migration cleanup using a Huly admin tool.
- [ ] `_originated:'gitlab'` marker persistence (R-3) — verify TxMixin `attributes._originated` is tx-transient and does NOT persist on the resulting Doc. If it persists, add to diff exclusion list in `mr-mixin-reader.ts`.
